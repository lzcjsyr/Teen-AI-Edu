# 2026-07-12 工作日志

## 表情包生成：老板离谱决策（嘲讽老板糊涂/异想天开/折磨打工人）

- 调用工作区技能 `微信动图表情技能`（三步确认工作流）。
- 三步完成：① 迷你故事脚本 → ② 文生图 2×2 分镜（用 ImageGen）→ ③ 脚本拼接 GIF。
- 项目目录：`/Users/dielangli/Desktop/AI青少年培训/表情包/老板离谱决策/`
- 成品：`最终成品/老板离谱决策.gif`（240×240，4帧，2500ms，约126KB），质检"白色背景通过"。
- 四格：在的 → 您说 → ？？？ → 好的（原创圆脸打工人，纯白底，无真人老板形象）。

## 踩坑记录（重要，下次复用）

1. **背景必须真纯白**：第一次分镜带米黄办公桌/地板，脚本"白色背景检查"失败（四角非 #FFFFFF）。
   解决：重出分镜，去掉 desk/floor，角色"悬浮"在纯白上，prompt 强调 "PERFECTLY FLAT SOLID PURE WHITE #FFFFFF BACKGROUND IN EVERY PANEL"。
2. **水印**：默认生成的图带「图片由AI生成」水印，会带入 GIF。重出时在 prompt 加 "NO WATERMARK"。
3. **Pillow 环境**：用托管 venv `/Users/dielangli/.workbuddy/binaries/python/envs/default/bin/python3`（已装 PIL 12.2.0）跑 `微信动图表情技能/脚本/拼接动图.py`。
4. **当前模型不支持看图**（Read 图片会过滤），无法肉眼质检，改用脚本的 `质量检查.json` + 像素采样验证四角白度。
